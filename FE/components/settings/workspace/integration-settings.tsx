"use client";

// ponytail: 다음 커밋이 기존 workspace-settings.tsx에서 연동 카드 3개를 옮긴다.
//           외부 시스템은 읽기 전용으로 바꾼다.
export function IntegrationSettings() {
  return <p className="mt-6 text-sm text-slate-400">연동 설정 — 준비 중</p>;
}

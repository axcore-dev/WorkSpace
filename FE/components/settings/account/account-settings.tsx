"use client";

// ponytail: 다음 커밋들이 섹션 6개(프로필·계정 보안·알림·기기·지원·사용자 ID)를 채운다.
//           지금은 라우트를 뚫고 정합성 테스트를 통과시키는 자리표시자다.
export function AccountSettings() {
  return <p className="mt-6 text-sm text-slate-400">계정 설정 — 준비 중</p>;
}

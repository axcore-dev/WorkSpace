# 빌드 컨텍스트에서 빼는 것들. 특히 .env* 는 반드시 빼야 한다 —
# Next.js 가 빌드 시점에 .env 를 읽어 NEXT_PUBLIC_* 값을 번들에 박기 때문에,
# 로컬 .env 가 들어가면 localhost 주소가 그대로 서버 이미지에 남는다.
# 서버용 값은 Dockerfile 의 ARG 로만 받는다.
node_modules
.next
out
coverage
.env*
.git
.gitignore
*.md
*.tsbuildinfo
npm-debug.log*
